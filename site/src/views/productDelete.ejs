<%-include("./partials/head", {title: 'Admin'})  %>
<%-include("./partials/admin")  %>



<div class="contain-prod-create">
    <div class="cont-prod-create">
        <div class="tit-product-create">
            <h5 class="products-title">Eliminar producto: <i><%= producto.name %> </i></h5>
        </div>
        <!-- Editar productos -->
        <div class="products-create">
            <form action="/products/delete/<%= producto.id %>?_method=DELETE" method="POST" enctype="multipart/form-data">
                <div class="prod-create-box">
                    <div class="">
                        <label for="name" class="form-label">Nombre del producto</label><br>
                        <input type="text" id="name" name="name" value="<%= producto.name %>" class="form-input">
                    </div>
                    <div class="">
                        <label for="price" class="form-label">Precio</label><br>
                        <input type="text" id="price" name="price" value="<%= producto.price %>" class="form-input">
                    </div>
                    <div class="">
                        <label for="categoria" class="form-label">Categoría</label><br>
                        <select type="text" id="categoria" name="categoria" class="form-input">
                            <option value="">Selecciona una opción</option>
                            <option value="jabon" <% if(producto.categoria == 'jabon'){ %> selected <% } %>>jabon</option>
                            <option value="shampoo" <% if(producto.categoria == 'shampoo'){ %> selected <% } %>>shampoo</option>
                            <option value="perfume" <% if(producto.categoria == 'perfume'){ %> selected <% } %>>perfume</option>
                            <option value="acondicionador" <% if(producto.categoria == 'acondicionador'){ %> selected <% } %>>acondicionador</option>
                        </select>
                    </div>
                    <div class="">
                        <label for="description" class="form-label">Descripción</label><br>
                        <textarea name="description" id="description" cols="100" rows="7"
                            class="form-input"><%= producto.description %> </textarea>
                    </div>
                    <div class="">
                        <label for="image" class="form-label">Selecciona aquí la imagen del producto</label><br>
                        <input class="text-input" name="image" type="file">
                    </div>
                </div>

                <!-- botones -->
                <div class="prod-create-button">
                    <div class="float-right">
                        <button type="submit" class="btn-guardar">Eliminar</button>
                        <button type="reset" class="btn-cancelar">Cancelar</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>